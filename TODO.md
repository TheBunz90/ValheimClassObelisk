# TODO

Bugs and changes we want to track and fix one at a time. Add new items to the bottom of Open; move finished items to Done with a one-line note on the fix (and mention it in CHANGELOG.md if it's user-facing).

## Open

## Done

- [x] **Perk rework, Phase 4: Archer and Wizard rewrites (final batch - all 9 classes now on the new system).** Full rewrite of both to the new 5-tier shape from `class-perks-rework.json`. Notable along the way: (1) Archer's old Arrow Slinger mutated shared item data (`weapon.m_shared.m_attack.m_drawDurationMin`, keyed by weapon *name*) to speed up bow draw - the same shared-mutation leak already fixed elsewhere in this rework (two players wielding the same bow model would leak the buff between them). Replaced with non-mutating Postfixes on `Humanoid.GetAttackDrawPercentage()` (bows) and the newly-discovered `ItemDrop.ItemData.GetWeaponLoadingTime()` (crossbows, confirmed via decompile as the actual read site vanilla uses for reload duration) that scale the *computed result* instead of the underlying shared field - Combat Rhythm (L30) now branches bow-draw-speed vs. crossbow-reload-speed cleanly through these two hooks. (2) Found and fixed two real bugs in Wizard's old code while rewriting it: its fire/frost damage-threshold trackers (`_frostDamage`/`_fireDamage`) were module-level statics shared across every player (one player's damage could trigger another player's aura) - moved to a per-player dictionary, same fix already applied to Brawler/Crusher/Lancer's stacking buffs; and its post-damage patch method was tagged `[HarmonyPrefix]` instead of `[HarmonyPostfix]` (a copy-paste from the prefix above it), meaning Essence Leech and the threshold trackers ran before the hit was finalized and could read a null `player` in the unconditional threshold check below it. (3) Wizard's new Archmage (L50) perk needed a third aura design for spirit/blood-magic damage (Frost Armor and Immolation Aura already cover fire/frost) - added a new Lifesteal Aura (heals 20% of all damage dealt while active, not just magic-weapon hits, mirroring Immolation's existing "autonomous aura" precedent) per a decision made during planning; blood magic's own Elemental Imbuement (L40) bonus deals spirit-type damage, so the existing spirit accumulator already covers "blood magic damage" too without needing a fourth separate tracker. Repurposed the now-otherwise-unused Eitr Weave icon for the new aura rather than adding a new asset, since Eitr Weave itself is now a flat damage bonus (`ClassCombatManager`) with no status effect of its own. (4) Wizard's Arcane Flow (L20) reduces `Player.m_eitrRegenDelay` (a plain per-instance field, confirmed via decompile - reset to its base value of 1s on every eitr spend) rather than a status-effect multiplier, refreshed every tick the same way Sword Master's Dancing Steel and Lancer's Spear Storm attack-speed buffs already are. **Superseded for Wizard** by the magic-class-split entry below - kept here as historical record of what shipped at the time; Archer is unaffected and still current. See CHANGELOG.md (1.1.0).
- [x] **Magic class split: Wizard (Elemental Magic only) + new 10th class, Warlock (Blood Magic).** Per a full redesign (`magic-class-implementation-notes.md`, `class-perks-rework-updated.json`) the user wrote after using the Phase 4 Wizard for a bit - splits the single combined Wizard class into two focused ones rather than tuning the combined version further. Warlock starts every player at level 0 (no XP migration, same precedent as Axemaster splitting out of Sword Master in Phase 1). Notable along the way: (1) confirmed via decompile that `Attack.GetAttackEitr()`/`GetAttackHealth()` already apply up to a 33% skill-based discount before the flat configured cost is charged - both classes' L40 Eitr-cost-reduction perks (Arcane Efficiency / Dark Efficiency) Postfix that method and multiply on top of the existing skill discount rather than replacing it. (2) `Player.GetTotalFoodValue(out hp, out stamina, out eitr)` turned out to be the single clean choke point for both classes' L20 food perks (Arcane Nourishment's +15% food Eitr, Blood Feast's +15% food health) - much simpler than trying to intercept the per-food decay loop it's computed from; Blood Feast's food-*regen* half instead reuses a vanilla extension point that was already sitting there unused, `SE_Stats.m_healthRegenMultiplier` (wired to `SEMan.ModifyHealthRegen`), the same mechanism the old Eitr Weave passive used for Eitr regen. (3) The biggest technical gap found: vanilla has no persistent "this creature was summoned by player X" concept at all - `SpawnAbility.m_owner` is transient (spawn-effect-local, never copied onto the summoned Character), and `Tameable.Command` (which makes a summon follow its caster) is the same generic method used for commanding any manually-tamed pet, not summon-specific. Built a best-effort heuristic instead of deferring (per a decision made during planning): record a `(player, time, position)` on every `SpawnAbility.Setup`, then tag the next `Character.Awake()` nearby within ~2s/~10m with a custom ZDO key - independent of vanilla's own network-authority ZDO owner - which three of Warlock's five tiers (summon damage bonus, Blood Pact's summon buff, Sanguine Reclamation's summon healing) read back via a shared `ResolveAttributedPlayer` helper. Known limitation, accepted as part of the best-effort scope: anything else that happens to use `SpawnAbility` for a player-triggered spawn would also get tagged. (4) Bloodwell charge tiers (standard vs. "very-high-sacrifice") intentionally read the weapon's *raw configured* health cost, not the skill-adjusted `GetAttackHealth()` value, so leveling Blood Magic skill can't accidentally change how many charges a cast is worth - matches the design doc's own stated reasoning. (5) The Obelisk's class button grid moved from 3x3 to 5x2 for the now-10 classes (a clean grid rather than an uneven 3x4 with an empty slot) - third time this exact renegotiation has happened (4x2→3x3 in Phase 1, now this). See CHANGELOG.md (1.1.0). Post-release, several summon-attribution/tracking bugs were found and fixed during the user's own playtesting: a Bloodwell charge-count buff and Archmage damage-number badge were added, Archmage's tracking was redesigned from 4 parallel meters to 1 rolling meter that resets on affinity switch and pauses while an aura is active, a `Humanoid.StartAttack` Prefix-vs-Postfix bug that double-counted Bloodwell charges was fixed, the custom summon-ownership heuristic was replaced with vanilla's own `Character.RaiseSkill`/`MonsterAI.GetFollowTarget()` mechanism (per the user's own observation that their Blood Magic skill was correctly leveling from skeleton kills), the Archmage buff icon not updating on affinity switch was fixed, and - the last and most involved fix - Staff of the Wild's stationary vine summon (which has neither a working follow-target link nor a `Tameable` component, unlike roaming summons) needed its own fallback: a hostile-faction filter on the ownership-correlation fallback (a wild Troll fighting the vine was getting permanently misattributed as the player's own summon) plus widening its correlation window from 2s to 15s (the vine's first real attack often landed after the original 2s window had already expired, so it could never resolve an owner at all), plus a skill-type gate on Wizard's Archmage tracking so a Warlock summon's damage can't feed a player's Wizard progress when both classes are active at once.
- [x] **Perk rework, Phase 3: Crusher, Sword Master, Lancer rewrites (1H/2H split batch).** Full rewrite of all three to the new 5-tier shape from `class-perks-rework.json`, each now branching on `ClassCombatManager.IsTwoHandedWeapon` (Crusher: 1H maces vs 2H hammers; Sword Master: 1H swords vs 2H greatswords; Lancer: spears vs the new `IsPolearmWeapon`). As predicted, this fixed Crusher's and Lancer's Level 10 double-damage-bonus bug for free by consolidating each flat bonus into `ClassCombatManager` only - the same rule already applied to Sword Master/Assassin. Notable along the way: (1) Crusher's Colossus moved from L50 to L20 and switched from mutating shared item data (`item.m_shared.m_movementModifier = 0f`) to the safer per-instance offset pattern established for Axemaster/Bulwark; Thundering Blows now branches 1H-heavy-only (3m/25%) vs. every-2H-hit (3m/20%, since 2H hammers have no Heavy Attack at all - perkReworkPlan.md's own motivating example); Earthshaker's post-stagger attack-speed proc detects "did this hit cause a stagger" by snapshotting `target.IsStaggering()` before the hit in the same prefix/postfix pair via Harmony's `__state`, rather than patching `Character.Stagger()` directly (which carries no attacker context). Dropped "Might of the Earth" (-30% stamina) entirely - not part of the new kit. (2) Sword Master's Riposte Training now branches 1H (+30% on any hit) vs. greatsword-special-only (+30% + 20% stagger); its parry detection switched from an inline per-call reflection lookup to Bulwark's cached-reflection `GetCurrentBlocker`/`WasTimedBlock` pattern. Dancing Steel (L50) is now a persistent +15% attack speed while a sword is equipped (via `AnimationSpeedManager`, refreshed every tick so it reacts to level-ups/gear changes) plus an independent +10% damage-for-5s on parry, stacking alongside L30's separate consumable Riposte bonus. Found and fixed a real bug before it shipped: the first draft of Dancing Steel's parry bonus computed a multiplier ratio against a stale pre-Riposte damage baseline, which would have *reduced* damage whenever Riposte had already modified the same hit - switched to mutating the hit directly instead, matching how Riposte itself already does it. Dropped "Counter Attacker" (block power/parry bonus) entirely. (3) Lancer's Spear Storm redesigned from a flat lightning-per-stack buff into a branching one: spear stacks grant attack speed (via `AnimationSpeedManager`), polearm stacks grant stagger damage - dropped the old `SE_SpearStorm` status-effect/icon in favor of the same silent per-player stacking-dictionary pattern already used by Axemaster's Rending Rhythm. Stormpoint (L40) reuses the freed-up lightning theme as a flat instant bonus. Impaling Momentum's (L50) "thrown spear hits beyond 15m" condition needed no new detection plumbing - a melee spear thrust can't physically reach 15m, so hit distance alone is a sufficient signal for "this was thrown," without needing to separately intercept the `Projectile` path. Balanced Grip's pickup-radius perk temporarily widens `Player.m_autoPickupRange` only for the duration of a single `AutoPickup` call (restored exactly via Harmony `__state`) rather than leaving it mutated, since thrown spears turn out to already be ordinary auto-pickup-eligible `ItemDrop`s in vanilla (confirmed via decompile - only manually player-dropped items get `m_autoPickup` cleared). Follow-up per feedback: replaced that pickup-radius approach entirely with a proper recall - thrown spears now always return directly to the Lancer's inventory the instant they hit anything (mob, terrain, another player), with a 10s fallback in case the throw never hits anything at all (flies into the void, off a cliff, etc.), rather than needing to be walked over. Implemented via `Projectile.Setup`'s postfix (starts the 10s clock at throw time, independent of the projectile's own fate) and a `Projectile.SpawnOnHit` prefix that nulls out `m_spawnItem` - the field vanilla itself uses to turn a landed thrown spear into a ground pickup, confirmed via decompile - before the original runs, so that one specific drop is skipped while every other on-hit effect (impact VFX, etc.) still fires normally; the recalled item is the exact same `ItemData` reference `Attack.ConsumeItem` removed from inventory when thrown (confirmed via decompile), so durability/quality carries over with no duplication risk from the normal hit path. Added a defensive null-out of `m_spawnItem` on the 10s-timeout path too, closing a narrow edge case where a still-alive, never-hit projectile could otherwise also trigger vanilla's own TTL-expiry ground-spawn after we'd already granted the item back, producing a duplicate. Per a decision made during planning, followed `class-perks-rework.json`'s Lancer redesign exactly, dropping two mechanics with no replacement: the old L10 stamina-cost reduction, and "Spear of Relocation" (teleport-to-target on thrown-spear hit, previously L50) - both intentional cuts, not oversights. See CHANGELOG.md (1.1.0). **Renamed post-release-prep:** this class shipped as "Axemaster" during development but was renamed to "Executioner" (and its own Level 50 perk, previously also named "Executioner", renamed to "Execute" to avoid the collision) just before the first public Thunderstore/Nexus package - every reference in code, the Obelisk UI, and CHANGELOG.md now reads "Executioner". Mentions of "Axemaster" elsewhere in this file are left as accurate history of what it was called at the time.
- [x] **Perk rework, Phase 2: Assassin, Brawler, Bulwark rewrites.** Full rewrite of all three to the new 5-tier shape from `class-perks-rework.json`. Notable along the way: (1) fixed Assassin's Level 10 double-damage-bonus (same pattern as the still-open Crusher/Lancer instances) by consolidating Cutthroat's flat bonus into `ClassCombatManager` only; (2) fixed a real bug in Assassin's Twist the Knife - it was applying its damage bonus to every hit, not just hits against poisoned targets as the perk describes; (3) fixed Brawler's Tough/Rage perks checking the *attacker's* Brawler level for damage-taken effects instead of the *target's* (backwards); (4) moved Brawler's combo counter and Rage state, and Bulwark's new Perfect Guard empower window, from module-level statics (shared across every player) to per-player dictionaries; (5) Bulwark's new "Perfect Guard" (parry-triggered) reuses Sword Master's existing timed-block detection (`m_blockTimer` reflection) rather than reinventing it; (6) Axemaster/Bulwark's new movement-penalty-removal perks (Woodsman's Carry / Shield Bearer) offset only their own item's contribution via `Player.GetEquipmentMovementModifier`, avoiding Crusher's existing approach of mutating shared item data (`item.m_shared.m_movementModifier = 0f`), which looks like it leaks the change to every player's copy of that item type; (7) Rage's trigger was simplified per feedback to fire on any damage taken, not just unblocked damage - the original "unblocked" check relied on `!player.IsBlocking()` at a patch point that runs before vanilla's own block resolution, an imprecise signal not worth the complexity; (8) added a new Silent Hands (L20) effect per follow-up request, then simplified per further feedback (the original "+25% crouch speed" was hard to notice while testing) to "move at normal walking speed while crouched" - a much more perceptible effect, and reads the player's own live `m_walkSpeed` rather than a fixed multiplier since walk speed (unlike jog/run) isn't itself scaled by equipment penalties in vanilla. `Character.m_crouchSpeed` turned out to be a plain per-instance field (not shared item data), read fresh every frame by the game's own movement code, so it's set directly rather than needing a patched getter. Refreshing on spawn + on-hit wasn't actually taking effect in testing, though; moved to a `Player.SetCrouch` postfix per follow-up feedback instead - `SetCrouch` is just `m_crouchToggled = crouch`, the exact moment the player toggles crouch on/off, so tying the refresh directly to that action is both more reliable and more correct than the reactive spawn/hit approach (still keeps the spawn hook as a harmless extra safety net; dropped the on-hit one entirely, since it no longer serves a purpose once the real trigger is wired up). Still not confirmed working as of the `SetCrouch` fix, so added temporary `DevLog` diagnostics (a Debug-only per-toggle log plus a once-a-second snapshot) and asked for a `LogOutput.log` capture - which immediately settled it: `m_crouchSpeed` WAS being set correctly and held its value (no bug in our logic at all), but the reference point was wrong for what was actually wanted. The perk was matching `m_walkSpeed` (the toggled walk-mode speed, already close to vanilla's own crouch speed - and in this install, reduced by Valheim Plus's movement-speed settings to ~1.6, slower than vanilla's default crouch speed of 2), not `m_speed` (the normal jog/run baseline, ~4 in the same log) - which is what was actually meant by "normal movement speed." Switched the perk (and its cached-before-`SetCrouch` value) to use `m_speed` instead, removed the now-served-their-purpose diagnostic logs, and updated the perk description to say "normal movement speed" rather than "normal walking speed" to avoid the same ambiguity going forward. (9) Rage's attack-speed buff was reported as making attacks feel *slower*, not faster. Root cause: vanilla's own unarmed combo animation already varies `Animator.speed` across a swing's phases (observed fluctuating between 1.0 and 2.0 via diagnostic logging), and `AnimationSpeedManager.ApplyIfNeeded` was overwriting that with a flat absolute value instead of scaling it - so any phase vanilla had already sped up past our target got knocked back down. Switched to multiplying relative to vanilla's current value instead of replacing it. That introduced a second bug caught by the same log-then-fix loop: `Animator.speed` is a persistent field vanilla only rewrites at certain phase transitions (not every tick), so on the ticks in between, reading it back as "the base" was actually reading our own prior output and re-multiplying it, compounding exponentially (confirmed via log - a clean geometric ×1.10 chain up to 5.19). Fixed by tracking the value we last wrote per-character and only rescaling when `Animator.speed` no longer matches it (meaning vanilla set a fresh base since); otherwise left alone. Also tuned Rage's numbers twice per feedback after playtesting the fix: damage bonus down from +20% to +10% (felt too strong once attack speed worked correctly), attack speed tried at +10% then settled back at +20% (felt too weak at +10% once the compounding bug was gone). See CHANGELOG.md (1.1.0).
- [x] **Perk rework, Phase 1: shared infrastructure + new Axemaster class.** First phase of the larger perk rework (see Open above for what's left). Added a reusable `ClassCombatManager.IsTwoHandedWeapon` primitive (mirrors vanilla's own `ItemDrop.ItemData.IsTwoHanded()`, confirmed via decompile) for future 1H/2H perk splits. Added a full 9th class, Axemaster (axes moved out of Sword Master, which previously treated them as its own weapon type) - new `AxemasterPerkManager.cs` with all 5 tiers (Chopper's Training, Woodsman's Carry, Rending Rhythm with a 1H-stacking/2H-battleaxe-special-attack split, Hemorrhage bleed DoT, Executioner), registered through `PlayerClass.cs`/`PlayerClassManager.cs`/`ClassObeliskMod.cs` (including reworking the Obelisk's class-button grid from 4x2 to 3x3 to fit a 9th button). Also added a small generic guard to `ClassCombatManager`'s shared damage-bonus patch (skip hits with no `m_skill` set) to stop a synthetic DoT tick like Hemorrhage's from being re-multiplied by the class damage bonus on every tick - the same class of bug fixed for Assassin's poison, caught before it could ship this time. See CHANGELOG.md (1.1.0).
- [x] **Dropped the flat +0.5%/level passive damage scaling from every class.** Balance change (not a bug): each class's `Get<Class>DamageBonus` in `ClassCombatManager.cs` previously added a universal `level * 0.005f` on top of its named tier perks (e.g. Sword Master's "Level 10: +10% sword damage"); that flat per-level scaling is now removed, leaving only the discrete, named tier bonuses each class already advertises in its perk description. Not referenced anywhere in player-facing text (only internal code comments), so no description text needed updating. See CHANGELOG.md (1.0.5).
- [x] **Assassin poison was fighting vanilla's own poison system, roughly double-applying damage.** Venom Coating manually ticked poison damage once per second via its own `Game.Update` loop, calling `target.Damage()` each time. Since that re-enters vanilla's own `Character.RPC_Damage` → `AddPoisonDamage` → `SE_Poison.AddDamage()` (confirmed via decompiling Valheim's `Character`/`SE_Poison`/`StatusEffect` classes), and `SE_Poison.AddDamage` is a "replace pool + reset timer" call, our manual ticks kept restarting vanilla's own concurrently-running 1-second tick - so the target was taking damage from both our tick and vanilla's own tick at roughly the same cadence. Fixed by computing the stack-scaled total once per knife hit and handing it to vanilla's `SE_Poison` a single time, letting vanilla own all the actual tick timing/damage/visuals/sound from there (duration is now damage-derived, `~2 + √(total×2)` seconds, matching every other poison source in the game, rather than a fixed 10s). Verified separately that vanilla's own tick cadence (`SE_Poison.m_timer`) is unaffected by repeated `AddDamage()` calls, so fast auto-attacking with a knife can't starve poison of ticks. Also fixed two bugs found while rewriting this code: the Envenomous movement slow was compounding incorrectly across stacks (it recaptured "original speed" from the already-slowed value on every stack instead of a stable baseline), and it was applying at any Assassin level instead of being gated to the Level 30 "Envenomous" perk as intended. See CHANGELOG.md (1.0.5).
- [x] **Arrow Slinger (and other bow/spear-only perks) proccing off unrelated hits.** Reported: Archer's Arrow Slinger buff was proccing while running through the Black Forest as Greydwarfs threw rocks, despite no bow being fired. Root cause: `ArcherPerkManager`'s (and identically, `LancerPerkManager`'s) `Projectile.OnHit` patch used a `FindProjectileOwner` helper that fell back to "whichever player is within 100m" whenever a projectile had no resolvable ZDO owner - true for any monster-thrown projectile (Greydwarf rocks, etc.) that happens to land on some other nearby creature, misattributing it to whichever player was standing nearby and proccing their bow/spear perks regardless of what actually hit what. This predates this session's changes (confirmed via `git log` - untouched by any of our recent commits). Fixed by reading the projectile's own `m_owner`/`m_skill` fields directly (set once in `Projectile.Setup` and authoritative - `m_owner` needed Harmony's `___m_owner` field-injection since it's private on the real class) instead of guessing, and explicitly gating on the shot/throw actually being the right skill type (Bows/Crossbows for Archer, Spears for Lancer). Deleted both now-dead `FindProjectileOwner` helpers. **Note:** a third, structurally-identical copy of this same pattern (`ClassCombatManager.ResolveProjectileOwner`) exists but is dead code - its only caller is inside a commented-out block - so it was left alone rather than touched unnecessarily; flag if you'd like it cleaned up too. See CHANGELOG.md (1.0.5).
- [x] **Fix level up logic.** `CheckForLevelUp` only ever compared XP against the very next level's threshold and advanced by exactly 1, so a kill with enough XP for multiple levels would get stuck one level behind until the next kill "caught it up". Now uses the existing `XPCurveHelper.GetLevelFromXP(totalXP)` (already used elsewhere for progress display) to set the level directly to whatever the total XP actually corresponds to. Also fixed `RPC_ReceiveClassXPAward`'s "New Perk Unlocked!" message, which only fired when the final level was itself a multiple of 10 - it now announces every perk tier (10/20/30/40/50) crossed during the jump, so a multi-level kill can no longer skip a perk-unlock notification. See CHANGELOG.md (1.0.5).
- [x] **Add Dual Class Notification.** There was previously no dedicated signal for this milestone at all - level 50 fired the same generic "Level Up!"/"Perk Unlocked!" toasts as every other level. Added a one-time "★ Dual Class Unlocked! ★" center-screen message in `RPC_ReceiveClassXPAward`, firing exactly when `CanSelectSecondClass()` flips false→true (the first class of the character to hit 50 - later classes reaching 50 won't repeat it). Also moved the Compendium's existing "A second class slot is available" note from the bottom of the "Active Classes" entry to the top, and made it bold/gold instead of gray, so it's the first thing seen rather than something to scroll past. Left the Obelisk panel's existing open-time hint text and perk descriptions unchanged per discussion (no advance-notice spoilers, no new persistent UI element). See CHANGELOG.md (1.0.4).
- [x] **Class XP persists across character deletion / reuse of a character name.** Root cause (confirmed via decompiling Valheim's own code): the main-menu character-preview model (`FejdStartup.SetupCharacterPreview`) is instantiated with `ZNetView` deliberately disabled, so it never gets a real ZDO and `Player.GetPlayerID()` always returns 0 for it - yet both browsing the character list and finishing "New Character" route through the same `Player.Save`/`Player.Load` calls our mod patches. That let one real character's class data get cached in memory under the shared key `0` while previewing it in the menu, then get baked straight into the next new character's first save - nothing to do with names or deletion, the leak happens in memory at menu time. Fixed by skipping both `Player_Save_Patch` and `Player_Load_Patch` whenever `GetPlayerID() == 0`. **Note:** this only stops the leak going forward - any character save that already inherited wrong XP before this fix stays wrong; no retroactive repair was done. See CHANGELOG.md (1.0.4).
- [x] **Second class slot (unlocked at level 50) can't actually be selected.** Root cause: the only wired-up selection method always replaced the active class list with one entry, and the correctly-eligibility-aware add/remove methods existed but were never called; the Obelisk UI was also a single-select radio pattern with no way to pick a second class at all. Fixed by adding `PlayerClassManager.ActivateClass`/`DeactivateClass` (add-if-room, swap if only 1 slot unlocked, block if both of 2 slots are full) and reworking the Obelisk panel: class buttons preview descriptions (green-highlighted when active), and a dynamic Activate/Deactivate button + italic "class limit reached" hint at the bottom drives the actual state change - all in-panel, since MessageHud toasts render underneath the panel and are invisible while it's open. See CHANGELOG.md (1.0.4).
- [x] **Remove extra notifications.** Catalogued every `player.Message` call in the mod (16 total). Removed the 8 that fired on or near every hit (chance-gated 10-30%, so still frequent): the universal "Weapon Mastery: +X% damage!" toast, Sword Master's Fencer's Footwork/Weakpoint, Archer's Long Shot/Adrenaline Rush/Magic Shot, Crusher's Cold Steel, and Assassin's Venom Applied - the underlying effects (damage bonuses, stamina restore, etc.) are unchanged, only the toast text is gone. Kept the per-kill "+X XP" toast and the rare, milestone/skill-gated ones (Level Up!, Perk Unlocked!, Riposte, Arrow Slinger buff, Thundering Blow!). Also deleted `ArcherPerkManager.ShouldConsumeArrow`, dead code found during the pass (never called - the reachable Magic Shot logic lives in `Inventory_RemoveItem_MagicShot_PreFix`). See CHANGELOG.md (1.0.4).
- [x] **Class-level damage bonus applied twice.** Two independent Harmony prefixes both patched `Character.Damage` with the same "+0.5%/level" scaling - one an orphaned earlier implementation (`ClassDamageBonusSystem.cs`, referenced nowhere else in the codebase) that had been superseded by `ClassCombatManager.cs` but never removed, plus a third stacking source in Sword Master's Riposte Training perk duplicating its own Level-10 tier bonus. Deleted the orphaned file and removed the duplicate flat bonus from Riposte Training (kept its distinct +25% proc). See CHANGELOG.md (1.0.4).
